#include "monty.h"
/**
 * queue - prints the top
 * @head: stack head
 * @line_num: line number in file
 * Return: no return
*/
void queue(stack_t **head, unsigned int line_num)
{
	(void)head;
	(void)line_num;
	locat.lifi = 1;
}
