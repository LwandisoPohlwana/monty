#include "monty.h"
/**
  *nop - dose nothing
  *@head: stack head
  *@line_num: lie number in input file.
  *Return: no return
 */

void nop(stack_t **head, unsigned int line_num)
{
	(void) head;
	(void) line_num;
}
